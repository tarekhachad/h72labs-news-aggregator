# Personalized News Aggregator

A personalized daily news tool, one of H72 Labs' two flagship products. A chat-style request interface ("give me today's news," with ad-hoc topic requests), backed by a scrollable feed where **each card is one specific story** (not a broad theme), synthesized from multiple sources — short summary by default, full report + sources on expand. Backed by a calendar history of past days, plus bookmarking of individual cards. Real signup/login from v1 (managed auth, not hand-rolled), with curated multi-select for topics and preferred sources at profile setup. Backend: tiered Claude API usage (Haiku for notability triage, Sonnet for card writing) plus free local embedding-based clustering, RSS feeds as the primary source (GNews as a supplement — NewsAPI is excluded, its free tier blocks non-localhost use). Hosted on Vercel + Supabase from day one. Full detail in `docs/`; full product context also lives in the vault root's `H72_Labs_Company_Plan.md`.

---

## Claude's Role in This Project

Vibe-code this end to end: turn the design docs into working code one small piece at a time, explaining each step since Tarek doesn't come from a software-engineering background. He digs deep and wants to fully understand what's being built — teach, don't just decide.

**Prime directive:** working end-to-end for Tarek himself and a handful of friends testing it — the chat-style "give me today's news" interaction, the scrollable feed of one-story-per-card synthesis, real login, calendar history, and bookmarking, per `docs/ROADMAP.md`. That's "shipped" for v1. Build Phase 1 (the core pipeline, hardcoded profile, no auth yet) before Phase 2 (real accounts) — prove the riskiest part cheaply first. If a session drifts into planning or polishing instead of moving toward the next roadmap phase, call it out and pull Tarek back to the smallest next build step. (He over-plans and under-ships — fight it here too.)

---

## Process

How work flows from idea to done in this project. Default for a vibe-coded build:
1. **Brainstorm** — conversation → design docs in `docs/`. Run the bundled `project-brainstorm` skill at `.claude/skills/project-brainstorm.md`. Ideally in Claude Code.
2. **Review** — read/annotate the design docs in Obsidian before building.
3. **Build** — Claude Code implements one small, explained piece at a time; commit as you go.
4. **QA pass per feature, fix, then re-verify — iterate to true convergence, no self-imposed cap** — after each feature (a roadmap phase, OR any meaningfully-sized addition) is implemented, before considering it done: spawn the `qa` subagent (`.claude/agents/qa.md`) against the new/changed code, then the `code-reviewer` subagent (`.claude/agents/code-reviewer.md`). Both have zero context at spawn time on how the feature was built or reasoned about — that's intentional, it's what makes the feedback unbiased rather than the implementer grading its own work. `qa` writes and runs real tests (or, for something better exercised live than unit-tested — like an API route or a UI flow — actually runs the app and hits it); `code-reviewer` reviews the diff itself for correctness/readability/performance/security.
   Fix what they find — then **re-run both subagents again against the updated diff**, not just self-verification with a throwaway script. **Every round means both agents, every time** — never drop to just one of the two (e.g. "code-reviewer only, this round is narrow/well-understood"), even for a round that only re-checks a specific fix. Scoping a round tightly (the specific finding + the diff resolving it, so it's fast and doesn't re-digest the whole feature) is fine; scoping it down to fewer agents is not — that's exactly the gap that let real bugs slip through unreviewed by one of the two lenses.
   **Keep iterating full rounds until one genuinely comes back with zero findings from both agents.** Do not decide in advance, before seeing a round's result, that "this will be the last round regardless of outcome" — that's a silent cap, not a stopping point earned by the evidence. The only legitimate way to stop before a round is fully clean is a **conscious, explicitly surfaced decision put to Tarek** ("round N found only X, a low-severity/cosmetic thing — keep iterating a full round, or accept and document?") — never inferred unilaterally from cost, session length, or "diminishing returns," since that judgment is his to make, not something to assume on his behalf. A fix is new code; it deserves the exact same unbiased two-agent pass the original code got, for as many rounds as it takes.
   Escalate to a full fresh cold review (both agents, no scope narrowing at all) rather than a tightly-scoped round whenever the fix touches something consequential — decision logic in the pipeline, data mutations, auth/security.
   This is a standing convention documented here, not a hook-enforced gate — it relies on actually following it every time a feature is "done," the same as any other rule in this file. It has been under-followed twice already, not from misunderstanding the rule but from quietly narrowing it under time/cost pressure: (1) 2026-07-30, a single-pass gate with no re-verification at all — a `writeCard.ts` allowlist fix shipped unreviewed and produced a real, user-visible bug the very next round, caught by Tarek's manual testing instead of a second automated pass; (2) 2026-08-04, during the front-page-ranking pipeline work — two full rounds each found and fixed real bugs (round 2 alone surfaced a high-severity Postgres deployment gotcha), then a third round quietly dropped to `code-reviewer` only and was privately decided in advance to be the last round regardless of what it found, without surfacing that as a choice to Tarek — who caught it and asked directly whether the loop had actually run to convergence or just been capped. Both incidents are the same failure shape: treating "this looks done" or "this is taking a while" as license to narrow the loop, instead of treating the loop's own output (a clean round) as the only thing that ends it. See `notes-logs/session-log.md`'s 2026-07-30 and 2026-08-04 entries for full detail on both.
5. **Sync the project's resume-ready description** — once a feature/phase clears the qa/code-reviewer loop above (same trigger: a roadmap phase, OR any meaningfully-sized addition), run the bundled `project-resume-sync` skill (`.claude/skills/project-resume-sync.md`) to update `Job Applications/Master Resources/projects/(C) personalized-news-aggregator.md` with what changed. This is what keeps `job-application-prep`'s resume bullets and cover-letter project descriptions current — that skill no longer reads this repo directly, only the synced file, so skipping this step means the next resume/cover-letter built off this project goes stale. Standing convention, not hook-enforced, same as step 4.
6. **Code review (broader)** — `/code-review` (user-invoked, Claude can't trigger it itself) is the heavier independent review for a full diff/branch — use it at bigger checkpoints (end of a phase), not as a per-feature substitute for the `code-reviewer` subagent above.

## Working in Claude Code vs. Cowork vs. Claudian

- **Claude Code (CLI)** is the primary tool for this project once building starts: `cd` into this folder and run `claude`. Full filesystem + git + run-code access.
- **Cowork** is fine for brainstorming/design-doc work and cross-domain pulls (e.g. feeding this project into a job application).
- **Claudian** for quick in-Obsidian questions about a doc while it's open.

## Folder Structure

```
Personalized News Aggregator/
├── CLAUDE.md          ← this file (project-scoped context)
├── docs/              ← design docs (ARCHITECTURE, TECH_STACK, ROADMAP, … — filled by project-brainstorm)
├── notes-logs/        ← project notes + append-only session-log.md
├── src/               ← code (or the repo root itself — TBD at brainstorm/build time)
├── .claude/skills/    ← bundled build skills (copies of root Skills/; master lives at the Brain root)
└── .obsidian/         ← Obsidian config (shared look/feel from the template)
```

> Bundled skills in `.claude/skills/` are **copies** of the Brain root's master `Skills/`. If a master skill changes, re-copy it here. They're bundled so Claude Code can run them from inside this standalone repo. Currently bundled: `project-brainstorm`, `project-resume-sync`.

## Rules & Conventions

- **`(C)` prefix** on AI-authored content/doc files (design docs, notes). Code files follow normal code conventions, not `(C)`.
- **Ask before editing** any non-`(C)` file Tarek wrote by hand.
- **Append-only session log:** record what was done / decided / what's next in `notes-logs/session-log.md`, newest at top — so context survives across Claude Code sessions.
- **git:** Tarek runs `git init` and manages commits himself; propose commit points but don't init or force git. Per the project-brainstorm handoff, `git init` happens once design docs are done, right before Phase 1 build starts.
- **Monetization (from H72 plan):** freemium — launch free with no ads to remove friction and build a retained user base first; ad cards and/or a paid ad-free tier come later once real usage data exists. Not a v1 concern.
- **Stack (from brainstorm):** Next.js on Vercel, Supabase for Postgres + managed auth. RSS feeds as the primary news source, GNews as a supplement — NewsAPI is a dead end (free tier blocks non-localhost deployment). Tiered Claude usage (Haiku triage, Sonnet writing) plus free local embeddings for clustering — don't default to "one big Claude call does everything," it's more expensive than it needs to be. Full detail + reasoning in `docs/TECH_STACK.md` and `docs/ARCHITECTURE.md`.
- **Data handling:** real user accounts from v1 (not just Tarek) — see `docs/DATA_HANDLING.md` before touching anything account/data-related.
- **Topic/source selection:** curated multi-select only for v1 (both topics and preferred sources) — free-text entry is explicitly deferred to v2, don't build it early.

## Current Status

> **Last updated:** 2026-08-11 (Phase 7 complete — both sub-phases finished, see the end of this section for the current summary; Phase 4/5/6 history below is kept for context, including references to `TopicBand`/`TopicNavBox`, which 5.3 later merged into `TopicNav`)
> **Status:** **Track B (frontend) B1-B5 done (2026-08-04/05).** Design tokens (Newsreader/Source Serif 4 serif-only typography, warm paper/ink palette — full provenance in `design-system/personalized-news-aggregator/MASTER.md`; the `ui-ux-pro-max-cli` tool's own automated blend proved unreliable for this niche category, used its direct style/color/typography lookups instead), shadcn/ui initialized (Base UI, not Radix), and the full static newspaper structure built: `Masthead`/`Sidebar`/`TopicBand`/`TopicNavBox`/`PageGrid`/`NewsCard`/`FrontPage`/`TopicPage` components, the `(paper)` route group (front page, `/topic/[slug]`, `/history/[date]`, `/history/[date]/topic/[slug]`). Real scope call put to Tarek and confirmed: the "Give me/Complete today's news" generation trigger lives on the front page only, not globally via the masthead (matches a real newspaper's daily-edition model). `/saved`/`/profile`/`/history` moved into the shared Masthead+Sidebar shell too — a genuine scope expansion beyond B5's original "restyle only" plan, confirmed with Tarek after qa found the alternative left the sidebar a dead end from those pages. Went through **3 full qa/code-reviewer rounds to genuine convergence** (no round narrowed or capped in advance): round 1 found a high-severity Sources-list clipping bug (fixed via an absolutely-positioned popover, since NewsCard's fixed grid-cell height couldn't accommodate an inline-expanding list) and the sidebar scope gap above; round 2 found a real CSS regression from round 1's own token cleanup (`button.tsx`'s secondary variant referencing a deleted custom property) and a real stuck-modal navigation bug (the sidebar's nav links didn't close the Sheet on click, since `/saved` etc. now share the same persistently-mounted layout — fixed via `SheetClose`); round 3 came back clean/non-blocking from both agents (one trivial comment fix applied, one style-convention question put to Tarek and explicitly accepted rather than decided unilaterally). Also caught and fixed, before review even started: a text-duplication bug that made every card overflow its grid cell and clip its own Save/Sources buttons out of view, and a Postgres deployment gotcha (same class as Track A's) where `persist_generated_cards` needed manual redeployment in this environment. 72 vitest tests (up from 55), `tsc`/`eslint`/`next build` clean throughout. Full blow-by-blow in `notes-logs/session-log.md`'s 2026-08-04/05 entry. Three disposable test accounts created this session, flagged for manual Supabase-dashboard cleanup: `b5test+1785891893937@example.com`, `qa-track-b-1785899709939@example.com`, `qa-track-b-1785899762975@example.com`.

**B6 also done (2026-08-05):** NewsCard's Sources popover replaced with a real CSS 3D flip via `motion` (Framer Motion). Went through **4 full qa/code-reviewer rounds to genuine convergence**, three of which found real bugs — each only surfaced because the next round reviewed the actual current code fresh rather than trusting the prior fix: round 1 (both card faces always mounted, `backface-visibility: hidden` doesn't remove the hidden one from tab order/accessibility tree — fixed via React 19's `inert` attribute); round 2 (that fix caused focus to be ejected to `<body>` per the `inert` spec when a keyboard user activated Sources — fixed via a `useEffect` moving focus to the newly-revealed face); round 3 (the round-2 fix's one-shot mount-guard wasn't idempotent under React Strict Mode's dev-only double-effect-invocation, stealing focus on every dev-mode page load — fixed via a `prevFlipped` ref comparison). Round 4 clean from both agents. Full blow-by-blow in `notes-logs/session-log.md`'s 2026-08-05 B6 entry.

**B7 also done (2026-08-05):** clicking a card's body opens a zoom/blur focus-mode overlay with the full report (`FocusOverlay.tsx`/`Backdrop.tsx`, Motion `layoutId` shared-element transition, `createPortal` to `document.body`), reusing the existing `/api/cards/[id]/expand` contract. Went through **3 full qa/code-reviewer rounds**, two of which found real bugs: round 1 found two overlays could open simultaneously and corrupt shared `inert` bookkeeping (fixed via a new `FocusModeContext` — one focused card at a time, enforced by construction), plus a focus-restoration race and a stale-prop refetch issue; round 2 found a regression in round 1's own Strict-Mode-double-fetch fix (permanently stuck loading state) and, more seriously, that closing the overlay before a slow real Claude call resolved and reopening fired a genuine second paid API call — fixed with a real architectural change, moving the whole report-fetch lifecycle out of `FocusOverlay` (unmounts on every close) into `NewsCard` (never unmounts across opens/closes). Round 3 clean from both agents plus one trivial cosmetic follow-up already applied. Full blow-by-blow in `notes-logs/session-log.md`'s 2026-08-05 B7 entry.

**B8 also done (2026-08-05) — Track B is now complete:** the full 3D page-flip transition between routes (front page ↔ topic page, topic ↔ topic), the last piece of Track B's animation-last ordering. New `src/lib/pageOrder.ts` (multi-hop flip-distance math) backs a client-orchestrated state machine (`PageTransitionContext.tsx`: `idle → zooming-out → flipping → zooming-in → idle`, gated on both a minimum flip-time and the destination route actually mounting) plus a portal-rendered abstracted-page visual (`PageTransition.tsx`, Motion). Went through **4 rounds to genuine convergence**: round 1 found three real gaps — a context-split re-render bug the code's own doc comments claimed was solved but wasn't (fixed via ref-backed `useCallback`/`useMemo` for a truly stable `navigate` identity), a missing `inert` accessibility sweep (the same reachability gap class already fixed twice, B6/B7 — fixed via new `PageTransitionInertBoundary.tsx`), and no stall-safety timeout if a navigation hung (fixed via an 8s `STALL_TIMEOUT_MS` fallback, later live-verified by qa actually hanging a real network request and confirming auto-recovery). Round 2's qa needed a relaunch after getting stuck trying to generate fresh test data instead of reusing existing accounts; code-reviewer then found the `inert` fix's own follow-on gap — focus stranded on `<body>` after a flip, the same bug shape B6/B7 already hit. Round 3 (targeted) fixed it by restoring focus to the transition boundary's own root; qa passed 19/19 live checks, code-reviewer's verdict was PASS with one non-blocking note (restored focus lands on the persistent Masthead wrapper before new page content, a keyboard-only UX nicety). Per this file's process rule, that non-zero-finding round was surfaced to Tarek explicitly rather than decided unilaterally — he accepted it and chose to document rather than iterate further. 79 vitest tests (up from 55), `tsc`/`eslint` clean throughout every round. Full blow-by-blow in `notes-logs/session-log.md`'s 2026-08-05 B8 entry.

**Phase 4 final integrated pass done (2026-08-05) — Phase 4 is now complete:** ran the Emil Kowalski design-engineering audit `ROADMAP.md`'s own 4.4 done-condition requires — installed the skill, ran `find-animation-opportunities` (restraint-first sweep, most candidates rejected by design), found 5 genuine gaps plus an out-of-scope styling gap (login/signup pages were never restyled with the app's design tokens, still default Tailwind zinc). Tarek chose to build all of them. Shipped: `SavedList.tsx` un-bookmark now animates out instead of vanishing; new digest cards fade/scale in with a stagger (gated so a reload never replays it); a bookmark-toggle pulse; `PreferencesForm.tsx` checkboxes finally show a selected state (previously none at all); login/signup restyled plus a real double-submit guard via the shared `SubmitButton`. Went through **3 full qa/code-reviewer rounds**, each finding something real: round 1 found a last-saved-card animation bypass (`AnimatePresence` unmounted before its exit animation could play), a non-animatable-`gap` visual artifact, a bookmark pulse firing on every mount instead of real toggles, and — qa's own independent find — a genuine SSR/reduced-motion hydration mismatch that the mount-pulse fix closed too. Round 2 found one low-severity trailing-margin regression, fixed immediately. Round 3 (tightly scoped, still both agents per this file's no-exceptions rule) surfaced one more near-imperceptible edge case, explicitly put to Tarek — accepted and documented rather than chased further. 79 vitest tests unchanged, `tsc`/`eslint` clean throughout. Full blow-by-blow in `notes-logs/session-log.md`'s 2026-08-05 final entry. **Next: Phase 5 (cost/quality pass + friends testing) — see `ROADMAP.md`.**

Prior status (2026-08-04, pre-Track-B-frontend): Phase 3 shipped and manually tested end to end; Phase 4 (ad-hoc requests) was scoped then deliberately deferred, roadmap renumbered so Phase 4 is the UI/UX design pass, split into sub-phases 4.1–4.4. **4.1 (brainstorm) and 4.2 (tooling) done** — see `docs/(C) UI_DESIGN.md` and `ROADMAP.md`'s 4.1/4.2 "Done" notes for full detail (newspaper-metaphor design; `ui-ux-pro-max-skill` → 21st MCP/shadcn → Motion tool order). **4.3 (Plan the build) now also done and approved:** a real Plan Mode round, grounded by two Explore passes (frontend: only 4 components exist today, zero nav/animation/icon libraries anywhere; pipeline: triage's output is discarded after logging, no score field exists on `Cluster`/`Card`/the `cards` table) and a Plan agent whose claims were spot-checked against source before trusting them. Resolved the brief's open questions: 4 discrete box-size tiers on a `grid-auto-flow: dense` CSS grid; topic pages become real routes (`src/app/(paper)/topic/[slug]/page.tsx`), not client-state tabs, for stable page identity across multi-page flips. Two-track plan: **Track A** (pipeline) grades triage to 1-5 `severity` + a new `src/lib/rank.ts` cross-topic ranking module (fail-open, one call per candidate pool, re-run every digest generation against the full cumulative pool so a later run's bigger story can dethrone an earlier front-page pick — confirmed with Tarek); **Track B** (frontend) goes tokens → shadcn primitives → static structure → animation last (Sources flip → focus mode → full page-flip, each verified before the next). Real scope change from the initial draft: `/history/[date]` will render the actual full newspaper for that date (not a plain list), reusing the front-page/topic-page components against that date's persisted final arrangement; `/saved` stays a simple list. Consciously deferred: the pre-existing unbounded triage concurrency risk stays unfixed (unrelated to this redesign). **4.4 (execute) is underway — Track A (pipeline) is done:** graded triage, `src/lib/rank.ts`'s cross-topic front-page ranking, and full route wiring are built, live-verified twice, and through four qa/code-reviewer rounds that each found and fixed real bugs (a rank-collision data-consistency gap closed by folding two writes into one atomic transaction, a Postgres function-overload deployment gotcha, a stranded-card edge case from an over-aggressive cost optimization that got reverted, and — after Tarek caught that a round had been quietly narrowed to one agent and pre-decided as final rather than run to genuine convergence — a real missed-call-site bug in `bookmarks.ts` that a proper unrestricted 4th round then found) — full blow-by-blow in `notes-logs/session-log.md`. This directly led to tightening this file's own step-4 process rule below: every round now requires both agents, with no privately-decided stopping point. 55 vitest tests passing, `tsc`/`eslint` clean. Committed separately from Track B, which hasn't started. **Full Track A/B implementation plan now lives at `docs/(C) IMPLEMENTATION_PLAN_4.4.md`** (copied there from the ephemeral Claude Code plan-mode artifact so it survives session restarts — read that file for full detail, this is only a summary). Next up: **Track B (frontend)** — tokens via `ui-ux-pro-max-skill` → shadcn primitives (21st MCP wasn't available when Track B started; Tarek is setting it up via `claude mcp add` — if it's live in the session that resumes this, use it, otherwise fall back to the standard `shadcn` CLI for the same end components) → static structure → animation last.

Design docs are in `docs/`: `ARCHITECTURE.md`, `TECH_STACK.md`, `ROADMAP.md`, `DATABASE_SCHEMA.md`, `GLOSSARY.md`, `DATA_HANDLING.md`. Phase 0 (scaffold), Phase 1 (RSS ingest → local embedding clustering → Haiku triage → Sonnet card writing → feed UI), and Phase 2 (real accounts) are done — see `notes-logs/session-log.md` for the full build log. Phase 2 highlights: signup/login/sign-out via Supabase Auth (`src/app/{login,signup}`, `src/app/auth/`), a curated multi-select onboarding flow (13 topics × 46 sources, no selection cap — expanded well beyond the original 2/3 during scoping, every feed live-verified), `user_topics`/`user_preferred_sources` tables with RLS (`supabase/schema.sql`), route protection via `src/proxy.ts` (Next.js renamed `middleware.ts` → `proxy.ts` in v16, migrated live) plus `/api/digest`'s own independent auth check, and `src/config/devProfile.ts` deleted now that real profiles drive the pipeline.

Same-day follow-up after real testing surfaced real problems: (1) the triage prompt's "head of state's briefing" framing had calcified into literal geopolitical-significance criteria that structurally excluded Sports/Finance/most topics — rewrote it to judge notability relative to each topic's own standard, and added topic-grouped tabs with explicit "no notable news today" empty states in `Feed.tsx`; (2) the "since last digest" recency-filter timestamp was keyed to the browser (`localStorage`) not the account, so switching test accounts in one browser leaked one account's history into another's supposedly-fresh first run — fixed by namespacing the key by user ID; (3) a real memory-crash incident — `cluster.ts` batched an entire large digest's articles (1000+, once topic/source selection has no cap) into one embedding call, and transformer batch padding made memory scale catastrophically (~34GB, crashed the machine); fixed by chunking into bounded batches of 64.

Two feature ideas raised during scoping (dynamic "hot topics" discovery, hierarchical subtopics like specific leagues/clubs/players) were deliberately deferred — documented with full reasoning in `ROADMAP.md`'s "Explicitly deferred" section, not lost. Also deferred: capping unbounded `Promise.all` concurrency in the triage step (risk of tripping Anthropic rate limits at large topic/source selections — 300-500+ clusters means 300-500+ simultaneous API calls today), and whether very large topic/source selections should have a sane cap at all.

**Profile/settings page (2026-07-30):** built `/profile` — edit topic/preferred-source selections (pre-populated, reusing the onboarding multi-select UI via a new shared `src/components/PreferencesForm.tsx`) and change password (`supabase.auth.updateUser`, no current-password reauth — Supabase doesn't require it and no reauth pattern exists elsewhere in the app, accepted as a documented trade-off). Refactored the onboarding save action's delete-then-insert DB logic into a shared `saveUserProfile()` in `src/lib/profile.ts` so onboarding and the new edit flow share one write path. Independent `qa` and `code-reviewer` subagent passes both caught a real bug: the delete-then-insert wasn't atomic and a duplicate-value submission (or an ordinary double-click, since neither form had a pending-disabled submit button) could wipe a user's saved preferences when the insert half failed after the delete half had already committed — fixed by deduping the zod input and adding a shared `SubmitButton` (`useFormStatus`) that disables while pending; verified live that both trigger paths no longer reproduce. Flagged but **not fixed** (new scope, not a bug in this feature): there's no forgot-password/email-reset flow anywhere in the app, so the password-change trade-off is bigger than documented — a hijacked session could lock a user out with no self-service recovery. Full detail in `notes-logs/session-log.md`.

**Phase 3 (2026-07-31):** digests now persist — one `digests` row per user per calendar date (appended to across same-day generation runs, not duplicated), `cards` rows linked to it, and a `bookmarks` join table. New: `/history` (date list) + `/history/[date]` (day view), `/saved` (bookmarked cards), and lazy expanded-report generation (a second Sonnet call, cached on first expand — the "full report on expand" half of the product's pitch, previously unbuilt). Home page now loads today's digest server-side and relabels its button ("Give me" → "Complete") once one exists. Full design reasoning, schema, and file list in `notes-logs/session-log.md`'s 2026-07-31 entry.

The qa/code-reviewer fix→re-verify loop caught real bugs this round, validating the loop tightened the same day it was formalized: round 1 found a high-severity concurrency race (double-click/two-tabs could double-generate a digest and double Claude spend), fixed with an atomic per-digest claim; manual re-testing then caught a second bug the subagents weren't specifically asked about — the claim wasn't being released after a *successful* run, because of a subtlety in how Node async generators interact with early-closing a stream. Round 2 (fresh cold review, since this touched concurrency) came back clean plus minor polish, applied. Consciously deferred (documented in `ROADMAP.md`, not silently dropped): the persist step's cursor advances even when some clusters failed transiently, so a transient failure now permanently excludes that story instead of just that one run.

**Post-Phase-3 manual testing (2026-07-31):** Tarek's testing pass — the explicit next step logged above — surfaced four things, all fixed and through two rounds of the qa/code-reviewer fix→re-verify loop: sources can now be viewed independently of generating the full report (`CardItem.tsx`, previously both were behind the same toggle); topic tabs no longer disappear on navigation for a topic with zero cards that day (the tab list now derives from stored preferences via a new `userTopics` prop, not just whichever cards happened to load); card ordering is now deterministic across reloads (added an `id` tiebreaker alongside `published_at` in both the DB query and the in-memory sort, since Postgres doesn't guarantee stable order for ties without one); and a new visual divider marks the boundary between cards from different same-day generation runs within a topic tab (`RunDivider.tsx`, reuses the previously-unused `cards.created_at` column via a new `Card.generatedAt` field, isolated into its own component so the upcoming design pass can restyle it in one place). **One outstanding manual step:** the updated `persist_generated_cards` Postgres function needs to be re-run by hand in the Supabase SQL editor (not urgent — the divider already works correctly without it; this just makes the DB value byte-exact instead of approximately equal).

**Cross-run duplicate detection (2026-07-31):** Tarek noticed re-running "Complete today's news" the same day could re-cover a story already on the digest — the since-cursor filter only excludes stale articles by publish time, with no notion of "already covered," so a follow-up article or a late outlet pickup sailed straight through as a new duplicate card. Fixed with a new dedup step between clustering and triage (`src/lib/dedup.ts`): a cheap embedding pre-filter against today's already-persisted card summaries, then one Haiku call to confirm borderline matches before a cluster is dropped — the same two-stage reasoning already documented in `ROADMAP.md`'s deferred section for the sibling same-*run* near-duplicate problem, applied here to the cross-*run* case. Two full cold-review rounds (this touches core pipeline decision logic) caught and fixed real issues: unbounded article text in the dedup prompt, the whole step being able to hard-fail digest generation on a transient DB/embedding error instead of degrading gracefully, and sequential instead of concurrent per-topic processing. Side effect worth knowing about: this project now has a real test runner (`vitest`, 25 tests across 5 files) — it had none before, since `qa` needed one to actually execute the tests it wrote for this feature.

**Ad-hoc requests scoped, then deferred (2026-07-31):** researched Phase 4 (chat-style one-off topic queries, e.g. "what's happening with the Fed today") before building it, and found the roadmap's original premise — "runs the same pipeline for that one topic" — doesn't actually hold: no RSS feed exists for a free-text query, and GNews (the docs' implied fallback for exactly this gap) was never built anywhere in the codebase. Worked out a better-scoped reframe instead of building a GNews client: a one-shot Claude call using Anthropic's server-side `web_search` tool, sidestepping the whole curated batch pipeline (which exists to sift a large multi-source scan, not answer one specific question) — estimated at roughly $0.04–0.05 per typical query, cheaper than the pipeline's own measured $0.17 full digest run. Tarek decided he wants a functional v1 with a good-quality UI before taking on this more architecturally different feature, so the full research (sourcing problem, what's reusable in the pipeline vs. not, the reframe, cost estimate, open UI/persistence/rate-limiting questions) was moved into `ROADMAP.md`'s "Explicitly deferred" section rather than lost, and the roadmap renumbered: **Phase 4 is now the UI/UX design pass** (was Phase 5), **Phase 5 is cost/quality + friends testing** (was Phase 6). Known gap, not fixed yet: this file's own opening product description above still lists "ad-hoc topic requests" as part of the v1 pitch — worth reconciling with the roadmap at some point, flagged but out of scope for the session that found it.

**Repo hygiene + Phase 4 tooling + sub-phase split (2026-08-01/03):** `.obsidian/` (388 tracked files — mostly the Claudian/Terminal plugins' own source, plus vault config, nothing to do with the app) was checked for sensitive content and found clean, then untracked via `git rm -r --cached` (files remain on disk, just no longer in git); committed and pushed. Two tools verified real and added to Phase 4: [nextlevelbuilder/ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill) (design-intelligence reference — styles/palettes/fonts/UX guidelines) and the [21st MCP](https://21st.dev/mcp) (generates/searches React+Tailwind components, fits this project's Next.js 16 + Tailwind 4 stack). Then, via a Plan Mode round, Phase 4 was split into `4.1` Brainstorm (visual identity/interaction model/UI behavior — conversational, Tarek's notes-driven), `4.2` Tooling/workflow/tech-stack structuring (conversational, open to tools beyond the ones already scoped), `4.3` Plan the build (a real Plan Mode round producing a file-level implementation plan), `4.4` Execute (implement + the standard qa/code-reviewer loop) — full detail in `ROADMAP.md`. The `(C) ROADMAP.md` sub-phase edit was committed in `6d9417a`.

**Closed (2026-08-03):** the `persist_generated_cards` Postgres function has been re-run by hand in the Supabase SQL editor — the DB value is now byte-exact, not just approximately equal.

**Phase 4.1 — UI/UX brainstorm (2026-08-04):** Plan Mode activated at session start despite 4.1 being scoped as conversational; flagged the mismatch to Tarek, who chose to exit Plan Mode and continue conversationally rather than produce a code-shaped plan. Tarek dumped his design notes; worked through the open questions one at a time via AskUserQuestion rather than guessing. Result, written to new file `docs/(C) UI_DESIGN.md`: a landscape digital-newspaper metaphor — masthead, a front page capped at 6 stories with newspaper-grid box sizing by importance, per-topic pages sized by within-topic importance, zoom/blur focus mode on card click (POV-camera alternative considered and rejected), flip-in-place "Sources" reveal distinct from focus mode, full 3D page-flip transitions between topics (flips through intervening pages when jumping via the topic dropdown), a hamburger-triggered overlay sidebar (bookmarks/history/settings/logout), serif typography throughout, light-only v1 (dark mode deferred), NYT/WSJ print edition as the visual anchor, and desktop/tablet-landscape-only scope (mobile deferred). Checked the actual pipeline (`triage.ts`, `schema.sql`) before proposing an importance-scoring mechanism rather than assuming one existed — none did (triage is a binary `notable` boolean, no severity column anywhere). Decided fix, flagged as real pipeline work rather than free UI styling: grade triage to 1–5 (topic-relative, same independence philosophy) for topic-page sizing, plus a new cross-topic ranking pass after triage for front-page selection/sizing (a topic-relative score isn't comparable across topics). Logged in `ROADMAP.md` under 4.1's new "Done" note so it lands in 4.3's implementation plan.

**Phase 4.2 — tooling/tech-stack structuring (2026-08-04, later same day):** checked `package.json` before recommending anything — no animation library, no shadcn/component primitives, no icon set, plain Next.js 16 + React 19 + Tailwind 4. That mattered because the 4.1 brief's zoom/blur focus mode and full 3D page-flip (with multi-page jumps) need real orchestrated animation, not CSS transitions alone. Settled an ordered workflow: `ui-ux-pro-max-skill` first (concrete palette/font-pairing/type-scale values against the brief's NYT/WSJ anchor) → 21st MCP generating shadcn/ui primitives (dropdown, Sheet/Dialog overlay sidebar, base components) restyled with those tokens → Motion (Framer Motion) hand-orchestrating the zoom/flip/slide animations, since no off-the-shelf library — including dedicated page-flip libraries — covers the specific zoom-out→flip→zoom-in choreography the brief calls for. Emil Kowalski's design-engineering skill stays 4.4-only; Higgsfield stays uncommitted (no known asset gap). Tech-approach questions settled via AskUserQuestion: shadcn/ui adopted over hand-building primitives (free accessibility/focus-trap handling); Motion adopted over CSS-only or a flip library. Settled without a question: `lucide-react` for icons (shadcn's standard pairing); Tailwind 4 is CSS-first, so tokens land in a `@theme` block in `globals.css`, not `tailwind.config.js`. Logged under 4.2's new "Done" note in `ROADMAP.md`.

**Phase 4.3 — Plan the build (2026-08-04, later still):** the real Plan Mode round. Two Explore passes first, to ground the plan in actual code: frontend has only 4 real components today (`Feed.tsx`, `CardItem.tsx`, `RunDivider.tsx`, `PreferencesForm.tsx`), zero nav/masthead/sidebar/animation-library/icon-library anywhere; pipeline has triage's output discarded after logging, no score field on `Cluster`/`Card`/the `cards` table. A Plan agent then produced the full design; its load-bearing claims (`Feed.tsx`'s `STAGE_ORDER` array, `src/proxy.ts`'s exact-path route matching) were spot-checked against source before trusting them, not taken on faith. Resolved the brief's two open questions: 4 discrete box-size tiers (hero/large/medium/small) mapped from severity/front-page-rank onto a 6-column `grid-auto-flow: dense` CSS grid (rejected template-areas and a masonry library — `dense` does free bin-packing, no new dependency); topic pages become real routes (`src/app/(paper)/topic/[slug]/page.tsx`) rather than client-state tabs, since the multi-page-jump page-flip needs a stable page identity a URL provides for free (plus back/forward nav and bookmarkability at no extra cost).

Three real scope calls put to Tarek directly rather than decided silently: **full cumulative front-page re-ranking every digest run** (union of today's persisted cards + this run's new ones, so a later run's bigger story can dethrone an earlier top-6 pick) over the simpler rank-once alternative; **`/history/[date]` renders the actual full newspaper for that date**, not a plain list — this changed the plan's real shape mid-session, since the initial draft had assumed a simple restyle, and reuses the front-page/topic-page components against that date's persisted "final arrangement" (which cumulative ranking already produces, no snapshot mechanism needed) — `/saved` stays a simple list, confirmed; and the pre-existing unbounded triage `Promise.all` concurrency risk **stays consciously deferred**, unrelated to this redesign.

Final plan: two tracks, only the types/schema step coupling them. **Track A** (pipeline) grades triage to 1-5 `severity` (same topic-relative/independent philosophy as today's binary, more granular), adds a new `src/lib/rank.ts` module (one Claude call per whole candidate pool — not per-item, so it structurally can't compound the existing concurrency risk — fail-open like `dedup.ts` rather than fail-closed like `triage.ts`), new nullable `cards.severity`/`cards.front_page_rank` columns, a new `update_front_page_ranks` RPC, a new "ranking" `DigestEvent` stage. **Track B** (frontend) sequences tokens (`ui-ux-pro-max-skill`) → shadcn primitives (21st MCP, real init needed — no `components.json` exists yet) → static structure (new `(paper)` route group + components) → animation last, in increasing-risk order (CSS Sources-flip → Motion focus-mode zoom/blur → the full page-flip, each built/verified before the next) — explicit state-machine design for the flip to avoid a visible pop on slow data fetches, and an explicit call not to build on Next 16's experimental View Transitions config. New vitest coverage mirrors existing test patterns (`dedup.test.ts`/`dedup-wiring.test.ts`'s mock styles); animation/visual correctness is explicitly scoped as live-verified via `qa` + manual review, not unit-tested, matching this project's existing convention. Full session detail in `notes-logs/session-log.md`; plan summary under 4.3's new "Done" note in `ROADMAP.md`.

**Phase 4.4 Track A — pipeline execution (2026-08-04, later still):** built types/schema plumbing (`Card.severity`/`frontPageRank`, matching nullable `cards` columns), graded triage (`src/lib/triage.ts` now returns `{notable, severity}` instead of a discarded boolean), and a new `src/lib/rank.ts` cross-topic front-page ranking module (one Claude call per whole candidate pool, fail-open), wired into `src/app/api/digest/route.ts` with a new "ranking" `DigestEvent` stage. Live-verified twice via Playwright against a real dev server and test accounts before review: a single-run check confirmed severity persists correctly end-to-end (9/9 cards), a two-run check confirmed the ranking stage fires every generation with no errors.

Then three rounds of the qa/code-reviewer fix→re-verify loop, each finding real bugs — validating the loop's value at real depth, not just a formality: **Round 1** (parallel qa + code-reviewer) both converged on the same real gap from different angles — two non-atomic writes (this run's new cards' ranks baked into one insert, existing cards' ranks updated via a separate RPC) could leave two cards simultaneously claiming the same front-page rank if the second write failed after the first succeeded; both also flagged ranking wastefully re-running on zero-new-card runs. Fixed by folding both writes into one atomic Postgres transaction and adding a skip guard. **Round 2** (fresh cold review, required since the fix touched data mutations) — both agents again independently found the same new bug (`parsed_output ?? []` silently returning an all-null array instead of the fail-open `null` sentinel on a truncated/unparseable response, wiping every existing rank); code-reviewer re-derived the round-1 atomicity fix from scratch and confirmed it genuinely correct; qa went further and caught two more real bugs the round-1 fix had introduced: a Postgres function-overload gotcha (re-running `schema.sql` after a signature change doesn't replace the old function, it adds a second overload — a real deployment trap since the old version was already live) and a stranded-card edge case (the zero-new-cards skip guard from round 1 could permanently prevent an already-failed-to-rank card from ever being reconsidered). Fixed all three — including **reverting** round 1's skip-on-zero-new-cards optimization entirely, back to the originally-approved "rank every run" design, since it was the actual root cause of the new bugs. **Round 3** (targeted, not another full fresh round, per this project's allowance once a pattern is well understood) confirmed all three round-2 fixes correctly close their gaps with no new issues.

Final state: 53 vitest tests across 10 files (up from 25/5), `tsc`/`eslint` clean throughout. Gave Tarek updated manual Supabase SQL-editor steps for the final function shape. Decided with Tarek: commit Track A now, separately from Track B, since it's a complete and independently-verified unit of work — bundling it with the unrelated frontend track would make for a harder-to-review diff. Committed as `7976306`.

**Loop-discipline correction + Round 4 (2026-08-04, post-commit):** Tarek asked directly whether that 3-round loop above had actually run to convergence or been capped — honest answer: capped. Round 3 had quietly dropped to `code-reviewer` only (no `qa`) and was privately decided in advance to be the last round regardless of outcome, neither surfaced to Tarek as a choice — the second time this exact failure shape happened (first: 2026-07-30's single-pass-gate incident). Updated the `qa_subagent_workflow_preference` memory with this second incident, then ran a genuine full round (both agents, unrestricted scope) to actually close out the committed work. `code-reviewer` came back clean (re-derived everything from scratch, one low-severity non-blocking note). `qa` found a real bug neither prior round's narrower file list had surfaced: `src/lib/bookmarks.ts`'s `getSavedCards` — a sibling query to `getDigestForDate` sharing the same `rowToCard` mapper — was never updated to select the new `severity`/`front_page_rank` columns, invisible to `tsc` because the code double-casts the joined row. Not live-visible yet (Track B hasn't shipped), but the exact "silently missed call site" shape from the 2026-07-30 incident, and would have bitten the moment Track B rendered those fields on `/saved`.

Per Tarek's request, tightened this file's own step 4 below (the qa/code-reviewer convention) to close the loophole: every round now requires both agents, no exceptions, and no privately deciding a round is final before seeing its result — both incidents documented there as the evidence. Fixed the `getSavedCards` bug plus two doc-only items (a stale `writeCard.ts` comment, a new `ROADMAP.md` deferred-risk bullet for `rank.ts`'s uncapped candidate-pool count), added a real regression test (`bookmarks.test.ts`, asserts the literal select string — the only thing that actually catches this bug class given the type-erasing cast), then ran a proper full re-verification round on that fix specifically. Both agents came back clean: `qa` swept the whole codebase for the same bug shape and found no other instance; `code-reviewer` independently confirmed the same plus a forward-looking (non-blocking) note for Track B to replicate this test pattern on any new `cards`-table query it adds. 55 vitest tests now, `tsc`/`eslint` clean. Committed separately from the original Track A commit.

~~**Next session:** Phase 4.4 Track B — frontend execution...~~ (superseded — Track B, and Phase 4 as a whole, are long done; see the current status below.) Still-open items from this era, all documented with reasoning in `ROADMAP.md`'s deferred section, still genuinely open as of this update: the forgot-password/email-reset gap, the same-run near-duplicate clustering-threshold question (now with a working sibling implementation in `dedup.ts` worth referencing if that direction gets reused), the optional-preferred-sources idea, and the deferred ad-hoc-requests research (including its own now-undecided UI placement question, per `UI_DESIGN.md`).

---

**Phase 5 ("Fix Iterations") opened, sub-phase 5.1 done (2026-08-06).** Tarek tested the shipped Phase 4 UI live and found 12 issues/requests. Planned via Plan Mode into a new Phase 5 in `ROADMAP.md`, 7 sub-phases (5.1-5.7) — full plan now lives at **`docs/(C) IMPLEMENTATION_PLAN_PHASE5.md`** (copied from the ephemeral plan-mode artifact so it survives session restarts, same convention as `IMPLEMENTATION_PLAN_4.4.md`). Old Phase 5 ("cost/quality pass + friends testing") renamed **Phase 6**.

**5.1 done** (topic-band centering + gap-free grid packing) — went through 3 qa/code-reviewer rounds and two full algorithm rewrites before converging on a "band" model (cards packed into strict same-tier groups, each closed before the next starts, so no band ever shares rows with a different one). Then a same-day follow-up: after seeing a real front page, Tarek asked for the tier system itself to change (drop the "large" tier, restructure hero/medium/small) rather than live with the original design's accepted trade-off — 3 more review rounds, one real bug found and fixed (hero could still share a band with *another* hero-tier card on topic pages, via severity ties) — then a correction after Tarek caught that the height design didn't match what he'd actually asked for (small was wrongly made 2 rows tall instead of 1), one more review round, converged clean. Full blow-by-blow across three 2026-08-06 entries in `notes-logs/session-log.md`. 96 vitest tests, `tsc`/`eslint` clean. Committed as `64fae1c`, on top of 5.1's first commit (`cceb648`).

**5.2 done (2026-08-07):** back arrow (Masthead, route-derived target via `getBackTarget()`), distinct Sources/Save/source-link hover states, flip-back-on-body-click for a flipped `NewsCard`, and the page-flip route transition disabled (`FLIP_ENABLED = false` in `PageTransitionContext.tsx`, reusing the reduced-motion bypass — flip code stays intact, just dead, for later). Went through **2 full qa/code-reviewer rounds to genuine convergence**: round 1 found a real keyboard event-bubbling bug (Enter/Space on the Sources/Save/Back buttons or a source link bubbled to the card's own handler before the element's own handler could act, since only `onClick` had `e.stopPropagation()` — fixed via a new `stopEnterSpacePropagation` helper wired to all four elements' `onKeyDown`) and a real UX gap code-reviewer flagged in the back arrow's target for a history date's own front page — put to Tarek directly rather than decided unilaterally, he chose `/history` (the date list) over the originally-planned `"/"`. Round 2 (both agents, fresh) came back clean, including qa's re-verification of the specific case round 1 hadn't explicitly tested (Enter on a focused, visible source link navigates correctly without flipping the card back).

**5.2 follow-up (2026-08-07, later):** Tarek asked for the back arrow to move — from alone on the header's far right to immediately right of the hamburger, both grouped on the left. `Masthead.tsx`'s header restructured from `flex justify-between` (with an invisible balancing spacer) to a `grid grid-cols-3` layout, so the title's column stays the header's exact structural center regardless of whether the left column holds one icon or two — no spacer needed. Pure layout change, no logic touched. One scoped qa/code-reviewer round (both agents, no exceptions even for a small change) came back genuinely clean: `code-reviewer` verified the grid-centering math against Tailwind's actual generated CSS and found no accessibility/height regression; `qa` confirmed pixel-exact title centering (640.0px both ways) both with and without the arrow, live via Playwright, 9/9 checks passing, zero console errors. 96 vitest tests unchanged, `tsc`/`eslint` clean throughout. Committed as `3a2074f` (bundled with the rest of 5.2).

**5.3 done (2026-08-07, later still):** merged `TopicBand` (front page) and `TopicNavBox` (topic page's old dropdown) into one `src/components/newspaper/TopicNav.tsx`, driven by an optional `activeTopic` prop — undefined reproduces the front-page flat centered list, set reproduces the topic-page role restyled to the same flat/inline look (no more dropdown), prepending a "Front Page" link and rendering the active topic as inert `aria-current="page"` text. `TopicPage.tsx`'s nav moved out of its row beside the `<h1>` into its own full-width band above it, matching `TopicBand`'s structural slot — sets up 5.4's shared nav-band height. Went through **2 full qa/code-reviewer rounds to genuine convergence**: round 1 found a real gap outside the original spec — a topic no longer in the user's current preferences (reachable via a stale bookmark, since the route never validates URL topic against current preferences) silently vanished from the nav instead of showing as "you are here," a real regression from the old dropdown's trigger button. Fixed with an unconditional inert-span render guarded against duplicating the normal case; live-verified directly against an orphaned topic. Round 2 clean (`qa` 15/15; `code-reviewer` found only a low-severity doc-comment nit, fixed directly, no third round needed). 96 vitest tests unchanged, `tsc`/`eslint` clean throughout.

**5.3 follow-up (2026-08-07, same day):** Tarek asked for "Front Page" to be highlighted so it reads distinct from the topic links beside it. Changed to `--color-accent` (masthead red) + `font-semibold`, matching exactly how `design-system/personalized-news-aggregator/MASTER.md` already scopes that token ("sparing use only — active nav"). One scoped qa/code-reviewer round, both clean — `code-reviewer` confirmed ~8.8:1 contrast (AAA) and consistency with the file's existing inline-style convention; `qa` confirmed via computed RGB values that only the Front Page link changed.

**5.2 follow-up 2 (2026-08-07, same day):** Tarek asked for the flipped-card "Back" button to get the exact same hover treatment as "Sources." Copied Sources' className onto Back verbatim (byte-identical). One scoped qa/code-reviewer round, both clean — exact RGB hover-color match confirmed live, flip-back functionality and layout unaffected.

**5.4 done (2026-08-07, same day):** sticky masthead + topic-nav band, via plain CSS `position: sticky` (per the plan's explicit instruction, not by restructuring `layout.tsx`). `Masthead.tsx`'s `<header>` gets `sticky top-0 z-20` plus an explicit opaque background; `TopicNav.tsx`'s `<nav>` gets `sticky z-10` with `top: var(--masthead-height)`, a new `globals.css` constant measured live via Playwright (73px, not guessed). Live-verified zero-pixel gap between the two bands and correct overlay layering (sidebar Sheet, focus-mode backdrop) before requesting review. One qa/code-reviewer round, both clean — `qa` re-confirmed the exact 0px gap across three route types plus hit-tested overlay layering via `elementFromPoint`, 16/16 passing; `code-reviewer` independently recomputed the 73px figure and traced the full z-index stacking order, verdict PASS WITH NOTES (three low-severity forward-looking observations, judged acceptable trade-offs for a desktop-only v1's static CSS constant, not fixed). 96 vitest tests unchanged, `tsc`/`eslint` clean throughout. Full blow-by-blow in `notes-logs/session-log.md`'s 2026-08-07 entries. Committed as `9e62ead`.

**5.5 done (2026-08-07):** card title (5-8 words) + 1-2 free-form color-coded label chips, generated by the same existing `writeCard.ts` Sonnet call. Nullable `cards.title`/`cards.labels` columns (migration applied by hand — standalone `alter table`, not an edited `create table`, per this project's own documented gotcha), threaded through `Card`/`CardRow`/`rowToCard` and all three `cards`-table read sites, rendered via a new `TITLE_CLASS` tier map and hash-colored chips (`src/lib/labelColor.ts`, FNV-1a mod a new muted 6-color palette). `FocusOverlay.tsx` (a deliberate small scope addition) now shows the real title instead of repurposing the summary paragraph as a fake headline. Flagged as the highest-risk remaining sub-phase (migration + prompt change + multi-file mapping chain) and reviewed at that bar: **4 full qa/code-reviewer rounds to genuine convergence**, three of which found real issues chased to correctness — round 1 found a title-length prompt-adherence gap (fixed, live-verified) and a zod guard that would hard-fail a card with zero retries (a real behavioral inconsistency with how every other recoverable anomaly in the file is handled); round 2 traced the retry-bypass from actual SDK source and fixed it by moving the guard to a soft JS-level trim/filter instead of differentiated retry logic; round 3 caught that the fix's own regression test never actually exercised the real zod schema (fully mocked SDK) and fixed it by exporting the schema for a direct `safeParse` test; round 4 (qa physically reintroduced the old buggy schema to prove the new test actually fails against it, then reverted) came back clean from both agents. 111 vitest tests (up from 96), `tsc`/`eslint` clean throughout. Full blow-by-blow in `notes-logs/session-log.md`'s four 2026-08-07 entries. Committed as `8a94067`.

**5.6 done (2026-08-07):** new `src/hooks/useDynamicLineClamp.ts` replaces 5.5's interim fixed per-tier line-clamp with real measurement — the pixel gap between the summary paragraph's top and the footer row's top, divided by computed line-height, applied via inline `WebkitLineClamp`, recomputed on a `ResizeObserver`. Live-verified: hero/medium tiers went from a flat conservative 4-line clamp to 6-9 lines depending on real title/label length, filling each box with zero overflow. **2 full qa/code-reviewer rounds to genuine convergence, plus a self-caught fix in between (not found by either agent's own testing):** round 1 (both agents, independently) found the same real bug — a one-shot `useRef` + empty-deps effect left the `ResizeObserver` watching a stale, detached node forever after a card's first focus-mode open→close cycle (the placeholder swap unmounts/remounts the whole face subtree), freezing that card's clamp against all future layout changes; fixed via state-backed callback refs so the effect re-attaches whenever the node identity changes. While independently live-verifying that fix, found a second and more serious bug: the fix's `getBoundingClientRect()`-based measurement is transform-aware, and closing focus mode plays a ~0.4s Motion `layoutId` shared-element return transition — the synchronous post-remount measurement fired mid-transition, captured an inflated visual rect, and (since `ResizeObserver` doesn't fire on `transform`-only changes) never self-corrected — live-reproduced a card's clamp permanently corrupting to 17-18 lines instead of 9. Fixed by switching to `offsetTop` (untransformed layout-box position, valid since `summary`/`footer` are direct children of `face`, their shared `offsetParent`). Round 2 (both agents, fresh, against both fixes together) came back clean — qa ran 3 consecutive round-trip cycles with zero drift, code-reviewer traced the full render tree and confirmed the `offsetTop`/`offsetParent` assumption holds. **Cost note:** chasing the bug through 7 debugging iterations, each with a fresh account and a deliberately broad (6-topic/15-source) fresh digest generation instead of reusing one session, burned enough Anthropic API credit that the balance ran out mid-session — Tarek flagged this directly; corrected for the rest of the session to one account/one digest reused across every check. 111 vitest tests unchanged, `tsc`/`eslint` clean throughout. Full blow-by-blow in `notes-logs/session-log.md`'s 2026-08-07 Phase 5.6 entry. Committed as `7035cf2`.

**5.7 done (2026-08-08) — Phase 5 is now complete (all seven sub-phases):** the run divider — a thin rule + "Added at H:MM" label between cards from different same-day generation runs. New `src/components/newspaper/RunDivider.tsx`; `src/lib/packGrid.ts` gained a `breakBefore` flag plus a new exported `packGridWithRunDividers` (detects a run boundary wherever `generatedAt` changes in the existing importance-sorted order — not grouped by run, since `rank.ts`'s cumulative re-ranking means a later run's card can legitimately interleave with an earlier run's by importance, and grouping by run first would have inverted that already-deliberate ranking behavior); `FrontPage.tsx`/`TopicPage.tsx` both wired to it. Real RSS timing meant a second real digest generation minutes after the first reliably found zero new notable articles (confirmed empirically, including after a 2-minute wait) — not just costly but actually unhelpful for testing — so live verification used one real generation plus client-side network interception (Playwright's `page.route()`) to inject synthetic second-run cards, exercising the real React path at zero extra API cost; this technique caught a real live React key-collision warning during round 1. **3 full qa/code-reviewer rounds to genuine convergence**, two of which found real issues: round 1 found a high-severity SSR/hydration timezone mismatch (`RunDivider` computed local time during render with no explicit timezone, reachable on an ordinary page load since `FrontPage.tsx` is server-rendered — fixed via `useSyncExternalStore`'s null `getServerSnapshot`), the React key collision described above (fixed by keying on `gridPosition.gridRow`, unique per divider by construction, instead of `generatedAt`), and a latent unenforced index-space mismatch between `packGridWithRunDividers`'s breakBefore computation and `packGrid`'s internal tier-sort (currently unreachable given both real callers' input happens to already be tier-monotonic, but fixed anyway by having `packGridWithRunDividers` pre-sort its own input first, making the coupling provably safe rather than empirically so). Round 2 came back clean from qa, "pass with notes" from code-reviewer (two cosmetic-only items — a comment overclaiming a non-existent behavioral distinction, and a test that pinned the bug's precondition but not the fix's replacement key — both fixed directly). Round 3 (tightly scoped, both agents, no live verification needed) came back clean from both. 25 vitest tests in `packGrid.test.ts` (up from 16), `tsc`/`eslint` clean throughout. Full blow-by-blow in `notes-logs/session-log.md`'s 2026-08-08 entry. Committed as `f6368e1`, bundled with a docs-only addition: a real UX gap Tarek hit live (digest "today" is a UTC calendar day, not the viewer's local day — confirmed and promoted from an old code-comment trade-off into a tracked `ROADMAP.md` deferred entry). All six of this session's commits (5.2 follow-ups/5.3 through 5.7) pushed to `origin/master`.

**Next session (superseded, see below):** ~~run `project-resume-sync` (Phase 5 as a whole is a resume-worthy addition — newspaper redesign, dynamic content fill, run dividers), then Phase 6 (cost/quality pass + friends testing) per `ROADMAP.md`.~~ Phase 6 was reassigned — see below, it's a new intermediary UI-tweaks phase, not the cost/quality one. Test accounts created during 5.1/5.2/5.5/5.6/5.7's live verification/qa passes, flagged for manual Supabase-dashboard cleanup — see the session-log entries for the specific email addresses (several `pna-*-test+...@example.com`, `qa-pna-51-...@example.com`, `qa-52-1786120774400@example.com`, several `qa-55*@example.com`, `qa-clamp-1786136878296@example.com` from 5.6, and several `qa-57*@example.com` from 5.7, not consolidated here since there are quite a few from repeated live-verification passes).

---

**Phase 6 ("UI tweaks") opened and completed, all five sub-phases (2026-08-11).** Tarek explored the shipped Phase 5 UI live and brought five notes: a suspected card-rearrangement animation bug, a request to disable topic-page nav before a digest exists, two spacing tweaks, and a request to make the focus-mode transition instant. Old Phase 6 ("cost/quality pass + friends testing") renumbered to **Phase 7**. Planned via Plan Mode: three Explore agents diagnosed the code first, a Plan agent designed the fix for each item, and the highest-risk claims were independently re-verified against actual source before the plan was finalized. Two decisions confirmed with Tarek before execution: topic-nav links **grey out** (not hide) when no digest exists yet; sub-phases are **risk-ordered** (spacing → instant transition → digest-gating → the animation-architecture fix), not the order originally reported. Full plan at **`docs/(C) IMPLEMENTATION_PLAN_PHASE6.md`**.

**6.1/6.2 (spacing, done):** `FrontPage.tsx`'s grid wrapper gained `pt-4` (fixes a zero-gap bug between `TopicNav` and the first card row, most visible on history front pages); `NewsCard.tsx`'s title/label-chips top margins increased (`mt-2`→`mt-3`, `mt-1`→`mt-2`), both above the dynamically-clamped summary so `useDynamicLineClamp` absorbs the change with no logic touched. One round each, both essentially clean — 6.1's `code-reviewer` flagged a cosmetic gap-asymmetry (48px live vs 16px history, since the live page has an extra button section in between) explicitly put to Tarek and accepted as-is; 6.2 came back clean from both agents including a hand-traced worst-case small-tier overflow check.

**6.3 (instant focus-mode transition, done):** `FocusOverlay.tsx`/`Backdrop.tsx` Motion transitions flattened to `{ duration: 0 }` (Tarek disliked the zoom/blur look), `layoutId` left structurally intact so the shared-element mechanism itself is untouched. One round, clean from both agents — `code-reviewer` independently re-verified the focus-management effects are genuinely commit-ordering-based, not timing-based; `qa` live-verified an 8-cycle rapid-toggle stress test and confirmed the B7 report-caching fix still holds.

**6.4 (disable topic-page nav before today's digest exists, done):** new `digestExistsToday?: boolean` prop (default `true`) on `TopicNav`/`TopicPage`, greying out sibling topic links via the same inert-span pattern already used for the active topic; history routes stay ungated via the default (verified, not assumed). Two rounds: round 1's `code-reviewer` found three real, non-blocking issues (an over-fetching existence check, a non-fail-open `Promise.all` member inconsistent with this project's `rank.ts`/`dedup.ts` convention, a soft `aria-disabled`-only accessibility signal) — all three fixed (new lightweight `digestExistsForDate()` in `digests.ts`, `.catch(() => true)` fail-open wiring, `role="link"` added). Round 2 clean from both.

**6.5 (fix the card-rearrangement animation bug, done) — the real fix, not cosmetic:** root cause was `NewsCard.tsx`'s `layoutId={`newscard-${card.id}`}` with no `<LayoutGroup>` anywhere in the app, so Motion ran one global shared-layout registry for every card site-wide — causing both a cross-page slide (a card shared between the front page's top-6 and its own topic page misread as "moved" when navigating between the two unrelated trees) and a same-page live-reflow animation (an already-mounted card repositioned by a live digest generation's grid repack animating instead of snapping). Fixed with `<LayoutGroup id={...}>` scoping the card grid per page instance in `FrontPage.tsx`/`TopicPage.tsx` (Motion transparently prefixes descendant `layoutId`s with the group id before using them as shared-node map keys) plus `layoutDependency={card.id}` on `NewsCard.tsx`'s grid-cell `motion.div` (constant per mounted instance, so ordinary repositions skip Motion's layout-change detection while the mount/unmount handoff to `FocusOverlay` — tracked unconditionally via `componentWillUnmount` — keeps the B7 zoom transition working). Flagged as the highest-risk sub-phase (core animation architecture) and reviewed at that bar: `code-reviewer` ran a full cold review independently tracing every correctness claim against the actual installed `framer-motion`/`motion-dom` runtime source — including the single most load-bearing claim, that a portaled `FocusOverlay` genuinely inherits the same `LayoutGroup` React context as its originating grid card since context propagates through the fiber tree regardless of `createPortal`'s DOM relocation. `qa` verified live with frame-precise `requestAnimationFrame` sampling (chosen over before/after snapshots, which can't reliably distinguish a hard state jump from a real interpolated slide) across cross-page navigation and a synthetic SSE-append reflow via network interception — zero interpolated frames either way, zero Motion console warnings. One round, genuinely clean from both agents.

120/120 vitest tests throughout (unchanged — this phase's changes are CSS/animation/live-DOM behavior, verified via qa's live Playwright passes per this project's existing convention that visual/animation correctness isn't unit-tested), `tsc`/`eslint` clean throughout every sub-phase. Full blow-by-blow in `notes-logs/session-log.md`'s 2026-08-11 entry.

**Next session:** run `project-resume-sync` (Phase 6's real animation-architecture bug fix plus UI polish is resume-worthy), propose a commit to Tarek, then Phase 7 (cost/quality pass + friends testing) per `ROADMAP.md`. Test accounts created during this session's live-verification passes, flagged for manual Supabase-dashboard cleanup — several `qa-fp-gap-*`, `qa-62-*`, `qa-focusmode-*`, `qa-topicnav-*`, `qa-round2-*`/`qa-debug*-*`, and `qa-65-*@example.com` accounts (see `notes-logs/session-log.md`'s 2026-08-11 entry for detail).

**Numbering convention change (2026-08-11, same day, post-commit):** "Phase 7" above (cost/quality pass + friends testing) is renamed to a fixed, non-numeric **"Final Phase (v1 launch)"** in `ROADMAP.md` — it's the actual last phase of v1 by design, so it no longer moves. Every further round of UI fixes/tweaks Tarek brings becomes its own new sequentially-numbered phase (7, 8, 9, …) inserted **before** the Final Phase, without ever renumbering anything already shipped (Phases 0-6) or the Final Phase itself.

**Phase 7 ("UI fixes") opened and completed, both sub-phases (2026-08-11, same day) — the new numbering convention's first phase.** Tarek tested the shipped Phase 6 UI live and brought two more issues. **7.1 (done):** the focus-mode CLOSE animation still played the old animated zoom-out even though Phase 6.3 made OPEN instant — root cause traced directly in `framer-motion`/`motion-dom` source (a shared-`layoutId` animation reads its transition config from whichever side is newly *mounting*, and `NewsCard.tsx`'s grid-cell had no explicit override for that path) — fixed with an always-present `layout: { duration: 0 }` sub-key, independent of the `isNew` entrance animation's `opacity`/`scale` keys. One clean round. **7.2 (done) — the real fix:** navigating away from `/` mid-generation and back showed an empty state instead of the progress bar resuming, and the topic-nav band wrongly appeared clickable during generation — root causes: generation state lived in `FrontPage.tsx`'s local `useState` (destroyed on every page-level remount, since `(paper)/layout.tsx` is the only layout for all sibling routes), and every "does a digest exist" check tested for a bare `digests` row instead of actual cards (`upsertDigestForToday` creates that row immediately, well before any card exists). Fixed with a new `DigestGenerationContext.tsx` (Provider + hook) mounted at the layout level, owning the generation state and fetch/stream loop relocated from `FrontPage.tsx`, surviving navigation like `PageTransitionProvider` already does; `digestExistsForDate` now requires a real card, not just a row. Flagged as the highest-risk sub-phase and reviewed at that bar: **3 full rounds, two of which found real bugs** — round 1 found a medium-severity staleness gap in `seed()` (didn't reset `loading`/`stageEvent`/`error`/`recentlyAddedIds` on a real date change, so a stale error could leak across the UTC day rollover) plus two low-severity robustness notes, all fixed; round 2 confirmed those fixes live (a genuine `Date`-override reproduction) but found a *new* issue the fix itself introduced — a `loadingRef`/state desync narrowly reachable if a generation spans midnight UTC during a navigate-away-and-back, explicitly put to Tarek rather than decided unilaterally, who chose to fix it (replaced the boolean guard with a per-date `inFlightDateRef`, discarding a stale run's late results instead of letting them corrupt a newer date's state); round 3 came back clean, including a live reproduction of the exact edge case via network interception confirming the fix holds. 120/120 tests, `tsc`/`eslint` clean throughout every round, plus a clean `next build`. Full blow-by-blow in `notes-logs/session-log.md`'s 2026-08-11 Phase 7 entry.

**Next session:** run `project-resume-sync` (the resume-on-navigate-back fix and its 3-round review chain are resume-worthy — a real React state-architecture bug, not just polish), propose a commit to Tarek, then continue watching for further UI-fix rounds (Phase 8, 9, …) before the Final Phase per `ROADMAP.md`. Many `qa-72-*`/`qa-r3-*`/`qa-round2-*`/`qa-debug-*` test accounts created across this session's live-verification passes, flagged for manual Supabase-dashboard cleanup — see `notes-logs/session-log.md`'s 2026-08-11 Phase 7 entry for detail.
