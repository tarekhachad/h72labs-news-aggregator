ANTHROPIC_API_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Admin-only, for `npm run invite`. Put these in a separate .env.admin (gitignored),
# NOT in .env.local and NEVER in Vercel: the secret key bypasses row-level security.
# SUPABASE_URL=
# SUPABASE_SECRET_KEY=
# INVITE_BASE_URL=https://news.h72labs.com
